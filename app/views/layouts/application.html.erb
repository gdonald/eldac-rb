<!DOCTYPE html>
<html>
  <head>
    <title>ELDAC - Electronic Data Capture</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="container mx-auto mt-12 px-5">
      <div class="border-b border-slate-200 mb-5">
        <%= form_with method: 'get', url: search_path, class: 'float-right' do |form| %>
        <%= form.text_field :q,
            placeholder: 'Search...',
            value: params[:q],
            class: 'px-3 py-2 bg-slate-100 border border-slate-300 rounded-md
              text-lg shadow-sm placeholder-slate-400 focus:outline-none
              focus:border-slate-300 focus:ring-1 focus:ring-slate-300
              disabled:bg-slate-50 disabled:text-slate-500 disabled:border-slate-200
              disabled:shadow-none' %>
        <% end %>
        <h1 class="font-bold text-5xl mb-4">
          <%= link_to 'ELDAC', root_path %>
        </h1>
      </div>
      <%= yield %>
    </main>
    <div class="container mx-auto border-t border-slate-100 mt-20 mb-10">
      <%= render 'shared/footer' %>
    </div>
  </body>
</html>
