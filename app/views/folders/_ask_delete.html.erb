<div class="modal fade" id="adm_<%= @folder.id %>" role="<%= :dialog %>" tabindex="<%= -1 %>">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button aria-label="Close" class="close" data-dismiss="<%= :modal %>" type="<%= :button %>">
          <span aria-hidden="<%= :true %>">&#215;</span>
        </button>
        <h4 class="modal-title">Confirm Folder Delete</h4>
      </div>
      <div class="modal-body">
        <p>Are you sure you want to delete the #{@folder.name} folder?</p>
      </div>
      <div class="modal-footer">
        <button class="btn btn-default" data-dismiss="<%= :modal %>" type="<%= :button %>">#{sg 'remove'} &nbsp;Close</button>
        <button class="btn btn-danger" id="<%= :delete %>" type="<%= :button %>">#{sg 'share-alt'} &nbsp;Delete</button>
      </div>
    </div>
  </div>
</div>
<script>
$(function(){ $('#delete').on( 'click', function() {
  $.ajax({
    method: 'delete',
    url: '/folders/#{@folder.id}'
  });
}); });
</script>
